ENVIROMENT == "mock" >> para indicar que voce quer gerar massa mocada
    SAVE == "archive" >> indica que vai gerar arquivos e enivar pro blob da azure
    --- caso queira arquivos, eu recomendo apagar os arquivos que existir na pasta /arquivos
    --- sempre que for gerar

ENVIROMENT == "qualquer coisa" >> vai enviar dados diretamente pra azure, simulando funcionamento padrao


o restante das info do .env está no trello, copiar e colar - caso deseje enviar pra nuvem direto e pra gerar arquivo tambem